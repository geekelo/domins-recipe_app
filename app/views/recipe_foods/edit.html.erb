<%= form_with(model: [@recipe, @recipe_food], local: true) do |form| %>
    <%= form.number_field :quantity, min: 1 %>
    <%= form.submit "Update Food" %>
  <% end %>
  
<%= link_to 'Cancel', recipe_recipe_foods_path(@recipe) %>
